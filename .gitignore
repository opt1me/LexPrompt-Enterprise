# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Environment
.env
.env.local
test_docs/

# The gateway's allowlist, copied from one of the tracked
# models.*.example.json files (docker-compose.yml mounts it read-only into
# the gateway container). Never committed: a real deployment's model list is
# environment-specific, and copying an example over it is how a developer
# ends up with one that differs from what's in git.
models.json

# Firebase deploy cache
.firebase/


# SDD execution workspace (scratch)
.superpowers/

# Agent worktrees — transient, created by isolated subagents
.claude/worktrees/

# Design handoff (owner-supplied prototypes, not part of the app)
design_handoff_lexprompt_redesign/

# Dev mTLS certs and keys generated by scripts/dev-certs.sh (Task 15) —
# never committed. A private key committed here is not fixable by a later
# commit.
certs/
*.pem
*.key
